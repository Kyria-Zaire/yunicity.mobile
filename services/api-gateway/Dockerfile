# Obsolète — l’image est construite avec docker/node-service.Dockerfile (racine du repo).
# Voir docker/README.md — commande : docker compose build api-gateway
FROM alpine:3.20
RUN printf '%s\n' "Utiliser docker compose depuis la racine (docker/node-service.Dockerfile)." >&2 && exit 1
